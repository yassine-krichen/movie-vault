@model Tp3.ViewModels.PaginatedListViewModel<Tp3.ViewModels.MembershipViewModel>

@{
    ViewData["Title"] = "Memberships";
}

<h1>Memberships</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Membership</a>
</p>

@if (ViewBag.Errors != null)
{
    <div class="alert alert-danger">
        <ul>
            @foreach (var error in ViewBag.Errors)
            {
                <li>@error</li>
            }
        </ul>
    </div>
}

<table class="table table-striped">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortBy="Id" asp-route-ascending="@(!Model.Ascending)" asp-route-page="1">
                    ID @if (Model.SortBy == "Id") { @(Model.Ascending ? "▲" : "▼") }
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortBy="SignupFee" asp-route-ascending="@(!Model.Ascending)" asp-route-page="1">
                    Signup Fee @if (Model.SortBy == "SignupFee") { @(Model.Ascending ? "▲" : "▼") }
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortBy="DurationInMonths" asp-route-ascending="@(!Model.Ascending)" asp-route-page="1">
                    Duration (Months) @if (Model.SortBy == "DurationInMonths") { @(Model.Ascending ? "▲" : "▼") }
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortBy="DiscountRate" asp-route-ascending="@(!Model.Ascending)" asp-route-page="1">
                    Discount Rate (%) @if (Model.SortBy == "DiscountRate") { @(Model.Ascending ? "▲" : "▼") }
                </a>
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Items)
        {
            <tr>
                <td>@item.Id</td>
                <td>$@item.SignupFee</td>
                <td>@item.DurationInMonths</td>
                <td>@item.DiscountRate%</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<nav aria-label="Page navigation">
    <ul class="pagination">
        <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
            <a class="page-link" asp-action="Index" asp-route-page="@(Model.CurrentPage - 1)" asp-route-sortBy="@Model.SortBy" asp-route-ascending="@Model.Ascending">Previous</a>
        </li>
        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-sortBy="@Model.SortBy" asp-route-ascending="@Model.Ascending">@i</a>
            </li>
        }
        <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
            <a class="page-link" asp-action="Index" asp-route-page="@(Model.CurrentPage + 1)" asp-route-sortBy="@Model.SortBy" asp-route-ascending="@Model.Ascending">Next</a>
        </li>
    </ul>
</nav>

<p>Showing @Model.Items.Count() of @Model.TotalCount memberships (Page @Model.CurrentPage of @Model.TotalPages)</p>
